import React from 'react';
import TitleCard from './TitleCard';
import ImageCard from './ImageCard';

const Card = () => (
  <div>
    <TitleCard />
    <ImageCard />
  </div>
)

export default Card;
